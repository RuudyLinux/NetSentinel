[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.19.1",
    "argon2-cffi>=25.1.0",
    "boto3>=1.40.0",
    "email-validator>=2.3.0",
    "fastapi>=0.141.1",
    "netmiko>=4.7.0",
    "psutil>=7.2.2",
    "psycopg[binary]>=3.2.0",
    "pydantic>=2.7",
    "pydantic-settings>=2.15.0",
    "pyjwt>=2.13.0",
    "python-multipart>=0.0.32",
    "pyyaml>=6.0.3",
    "reportlab>=5.0.1",
    "sqlalchemy>=2.0",
    "uvicorn[standard]>=0.52.4",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.4",
    "types-pyyaml>=6.0.12.20260815",
]

[tool.ruff]
line-length = 100
target-version = "py313"
# Alembic autogenerates these; their formatting is Alembic's template, not ours to lint.
exclude = ["alembic/versions"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.per-file-ignores]
# FastAPI's dependency injection requires the literal Depends(...) call as the
# argument default -- that's what FastAPI inspects at route-registration time, not
# a real "function call in a default" bug. Scoped to the API layer only.
"app/api/**.py" = ["B008"]

[tool.mypy]
python_version = "3.13"
strict = true
plugins = ["pydantic.mypy"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
filterwarnings = [
    # Emitted by starlette's test client on import because we pin httpx, not httpx2.
    # Third-party and unavoidable without changing a pinned dependency; scoped to this
    # one message so any other deprecation still surfaces.
    "ignore:Using .httpx. with .starlette.testclient. is deprecated",
]
